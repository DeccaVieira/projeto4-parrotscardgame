const qtdeCartas = prompt("Digite a quantidade de cartas que deseja jogar:");

function QuantidadeCartas(){
    if(qtdeCartas <= 14 && qtdeCartas>=2 && qtdeCartas%2==0){
console.log("é par");
    }
QuantidadeCartas(qtdeCartas);

const cartas = [];
while(cartas < qtdeCartas){
    
}
//garantir que o usuário digite um número par
//criar um array que puxe a quantidade de elementos que o usuário digitou - 1 com o push
//alinhar esses elementos com o justify-content
